<Window x:Class="dvmconsole.TabManagerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        Title="Tab Manager" Height="520" Width="760" MinHeight="420" MinWidth="620"
        WindowStartupLocation="CenterOwner"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}" TextElement.FontWeight="Regular" TextElement.FontSize="13"
        Style="{StaticResource MaterialDesignWindow}" Background="{DynamicResource MaterialDesignPaper}" FontFamily="{DynamicResource MaterialDesignFont}"
        Icon="/dvmconsole;component/Assets/select.png">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,14">
            <TextBlock Text="Tab Manager" FontSize="20" FontWeight="Bold" />
            <TextBlock Text="Choose which codeplug zones appear across the top. Hidden zones stay off until shown again."
                       TextWrapping="Wrap" Margin="0,4,0,0" />
        </StackPanel>

        <Border Grid.Row="1" Background="#333333" CornerRadius="5" Padding="12" Margin="0,0,0,12">
            <TextBlock Text="Tip: hide the rarely-used zones, then use the More tab on the main console when you need to bring one back."
                       TextWrapping="Wrap" Foreground="White" />
        </Border>

        <DataGrid x:Name="ZoneGrid"
                  Grid.Row="2"
                  ItemsSource="{Binding Zones}"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  CanUserDeleteRows="False"
                  HeadersVisibility="Column"
                  IsReadOnly="False"
                  SelectionMode="Extended"
                  SelectionUnit="FullRow"
                  GridLinesVisibility="Horizontal"
                  Margin="0,0,0,12">
            <DataGrid.Columns>
                <DataGridCheckBoxColumn Header="Show"
                                        Binding="{Binding IsVisible, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                        Width="80" />
                <DataGridTextColumn Header="Zone" Binding="{Binding ZoneName}" IsReadOnly="True" Width="*" />
                <DataGridTextColumn Header="Channels" Binding="{Binding ChannelCount}" IsReadOnly="True" Width="90" />
                <DataGridTextColumn Header="Streams" Binding="{Binding WebStreamCount}" IsReadOnly="True" Width="90" />
            </DataGrid.Columns>
        </DataGrid>

        <Grid Grid.Row="3">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <TextBlock x:Name="StatusTextBlock"
                       VerticalAlignment="Center"
                       Foreground="#FFCC00"
                       TextWrapping="Wrap"
                       Margin="0,0,12,0" />

            <StackPanel Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="Show All"
                        Width="96"
                        Margin="0,0,8,0"
                        Click="ShowAll_Click"
                        Style="{StaticResource MaterialDesignPaperDarkButton}" />
                <Button Content="Hide All"
                        Width="96"
                        Margin="0,0,24,0"
                        Click="HideAll_Click"
                        Style="{StaticResource MaterialDesignPaperDarkButton}" />
                <Button Content="Cancel"
                        Width="96"
                        Margin="0,0,8,0"
                        Click="Cancel_Click"
                        Style="{StaticResource MaterialDesignPaperDarkButton}" />
                <Button Content="Save"
                        Width="96"
                        Click="Save_Click"
                        Style="{StaticResource MaterialDesignPaperDarkButton}" />
            </StackPanel>
        </Grid>
    </Grid>
</Window>
